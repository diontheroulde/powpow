<h1><%= @instructor.name %></h1>

<div class="student-list">
  <p>Students:</p>
  <ul>
    <% @instructor.users.each do |user| %>
      <li><%= link_to user.full_name, user_path(user) %></li>
    <% end %>
  </ul>
</div>